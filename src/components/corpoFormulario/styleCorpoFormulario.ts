import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    corpoFormulario: {
    width: '100%',
    backgroundColor: '#f2f2f2',
    borderRadius: 10,
    gap: 10,
    padding: 20,
  },
});
  

export { styles };