import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  formularioContainer: {
    width: '100%',
  },
  cardAlturaCaminhao: {
    width: '100%',
    minHeight: 150,
    overflow: 'hidden',
  },
});

export { styles };