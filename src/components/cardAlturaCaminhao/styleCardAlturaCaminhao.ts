import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  formularioContainer: {
    width: '100%',
  },
  cardAlturaCaminhao: {
    width: '100%',
    minHeight: 150,
    alignItems: 'center',
    justifyContent: 'center',
    flexDirection: 'row',
    overflow: 'hidden',

  },
});

export { styles };