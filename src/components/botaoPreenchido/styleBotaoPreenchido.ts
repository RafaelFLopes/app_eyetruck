import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    button: {
    backgroundColor: '#123c52ff',
    padding: 18,
    borderRadius: 7,
    alignItems: 'center',
  },
  textButton: {
    color: '#f2f2f2',
    fontSize: 16,
    fontWeight: 'bold',
  },
});
  

export { styles };