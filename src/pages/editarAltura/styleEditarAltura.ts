import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    containerEditarAltura: {
        flex: 1,
        backgroundColor: '#f2f2f2',
        alignItems: 'center',
        justifyContent: 'center',
        padding: 15,
    },
    imagemEditarAlturaFormulario: {
        alignItems: 'center',
        },
        imagemEditarAltura: {      // Diminui o tamanho da logo
        height: 100,
    },
});

export { styles };